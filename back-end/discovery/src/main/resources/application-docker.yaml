server:
  port: 8010

spring:
  devtools:
    restart:
      enabled: true
  application:
    name: discovery

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://discovery:8010/eureka